// Глобальными называются переменные, которые объявлены вне блока. 
// Они имеют статическую продолжительность жизни, т.е. создаются при запуске программы и уничтожаются при её завершении.
// Глобальные переменные имеют глобальную область видимости (или «файловую область видимости»), т.е. их можно использовать в любом месте файла, после их объявления.


#include <iostream>

int g_v(4); 

int main()
{
    int g_v = 8; 			// Локальная переменная
    std::cout << "Global value: " << ::g_v;     //  (4)
    std::cout << "Local value: " << g_v;        //  (8)
    return 0;
} // Локальная переменная
